import React from "react";

type TParamsProps = {
  params: {
    postId: string;
  };
};

const PostDetailsPage = ({ params } : TParamsProps) => {
  return <div>
    
  </div>;
};

export default PostDetailsPage;
